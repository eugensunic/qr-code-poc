
- must fix vs code Formatting!
- clean yellow warnings before production


BUGS
- wrong link when scanning QR code
- find a way on how to fix warnings including objects into dependency array causes constant re-rendering
- config: local vs dev environment REACT
- footer positioning still not working good
- find better icons for languages (instead of cro.png & uk.png)
- visitor page not fetching image
- putting new image on edit results in wrong path when scanning the qr code


TODO
- find something to remove unused imports
- qrCode remove on sm