TODO
----
- see why history.push doesn't work in throws an error saying that push is undefined.
- re-evaluate JWT token (test combinations with it)
- implement error handler in middleware to pass next in node.js if error occurs instead of res.status(500).end()
- implement extension of JWT token
- put all url's in config file (REACT.js)
- 401 vs 403 re-evaluate
- after change password submit, logout the user
- store url of generated QR code to the database (important)
- clear inputs after submit

TODO (nice to have)
----
- typescript FE+BE
- refactoring folder structure
- rename variables
- see if import works instead of require

DONE
----
- check if multiple emails can be entered to the database --> they cannot
- backend user-registration needs findOne instead of find --> replaced
- fix layout on deleting item in overview section --> fixed
